---
#
# THIS FILE IS AUTO-GENERATED.
# DO NOT EDIT MANUALLY.
#
description: Découvrez l’objet `event` du trigger Actions post-change-password, qui fournit des renseignements contextuels sur son exécution.
trigger: post-change-password
pageType: event
title: 'Objet Event'
sidebarTitle: 'Objet Event'
---

L’objet `event` du trigger Actions post-change-password fournit des renseignements contextuels sur son exécution.

```json Event Object
{
  "secrets": {},
  "connection": {
    "id": "string",
    "name": "string",
    "strategy": "string"
  },
  "request": {
    "geoip": {},
    "ip": "string",
    "method": "string"
  },
  "tenant": {
    "id": "string"
  },
  "user": {}
}
```

<div id="eventauthentication">
  ## `event.authentication`
</div>

<ResponseField name="event.authentication" type="object" post={["optional"]}>
  Détails sur les signaux d’authentification supplémentaires obtenus lors du flux de changement de mot de passe.

  <Expandable title="propriétés de event.authentication" defaultOpen>
    <ResponseField name="riskAssessment" type="object" post={["optional"]}>
      Détails des évaluations du risque pour différents flux.

      <Expandable title="propriétés de riskAssessment" defaultOpen>
        <ResponseField name="supplemental" type="object" post={["optional"]}>
          Signaux supplémentaires envoyés par des fournisseurs tiers pour faciliter les évaluations du risque.

          <Expandable title="propriétés de supplemental" defaultOpen>
            <ResponseField name="akamai" type="object" post={["optional"]}>
              &#91;Accès anticipé&#93; Évaluation supplémentaire du risque. Cette fonctionnalité est offerte uniquement si Akamai Account Protector est activé et qu’Akamai transmet les en-têtes de la transaction.

              <Expandable title="propriétés de akamai" defaultOpen>
                <ResponseField name="akamaiBot" type="object" post={["optional"]}>
                  Résultats de détection des bots transmis par Akamai Bot Manager.

                  <Expandable title="propriétés de akamaiBot" defaultOpen>
                    <ResponseField name="type" type="string" post={["optional"]}>
                      Type des résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="action" type="string" post={["optional"]}>
                      Action indiquée dans les résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botCategory" type="array of string" post={["optional"]}>
                      Catégorie de bot indiquée dans les résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botScore" type="number" post={["optional"]}>
                      Score de bot indiqué dans les résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botScoreResponseSegment" type="string" post={["optional"]}>
                      Segment de réponse du score de bot indiqué dans les résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botnetId" type="string" post={["optional"]}>
                      ID du botnet indiqué dans les résultats d’Akamai Bot Manager.
                    </ResponseField>
                  </Expandable>
                </ResponseField>

                <ResponseField name="akamaiUserRisk" type="object" post={["optional"]}>
                  Résultats de détection du risque utilisateur transmis par Akamai Account Protector.

                  <Expandable title="propriétés de akamaiUserRisk" defaultOpen>
                    <ResponseField name="action" type="string" post={["optional"]}>
                      Action de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="allow" type="number" post={["optional"]}>
                      État d’autorisation de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="emailDomain" type="string" post={["optional"]}>
                      Domaine de courriel de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="general" type="dictionary" post={["optional"]}>
                      Risque général de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="ouid" type="string" post={["optional"]}>
                      OUID de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="requestid" type="string" post={["optional"]}>
                      ID de requête de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="risk" type="dictionary" post={["optional"]}>
                      Risque de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="score" type="number" post={["optional"]}>
                      Score de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="status" type="number" post={["optional"]}>
                      État de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="trust" type="dictionary" post={["optional"]}>
                      Niveau de confiance de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="username" type="string" post={["optional"]}>
                      Nom d’utilisateur.
                    </ResponseField>

                    <ResponseField name="uuid" type="string" post={["optional"]}>
                      UUID de l’évaluation du risque utilisateur d’Akamai.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventconnection">
  ## `event.connection`
</div>

<ResponseField name="event.connection" type="object">
  Détails sur la connexion utilisée dans le cadre de la transaction en cours.

  <Expandable title="propriétés de event.connection" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant unique de la connexion.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      Métadonnées associées à la connexion.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de la connexion utilisée pour authentifier l’utilisateur (par exemple, `twitter` ou `some-g-suite-domain`).
    </ResponseField>

    <ResponseField name="strategy" type="string">
      Le type de connexion. Pour les connexions sociales, `event.connection.strategy === event.connection.name`. Pour les connexions d’entreprise, la stratégie peut être `waad` (Windows Azure AD), `ad` (Active Directory/LDAP) ou `auth0` (connexions à une base de données), entre autres.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventcustom_domain">
  ## `event.custom_domain`
</div>

<ResponseField name="event.custom_domain" type="object" post={["optional"]}>
  Détails sur le domaine personnalisé associé à la transaction en cours.

  <Expandable title="Propriétés de event.custom_domain" defaultOpen>
    <ResponseField name="domain" type="string">
      Le nom de domaine personnalisé.
    </ResponseField>

    <ResponseField name="domain_metadata" type="dictionary">
      Les métadonnées du domaine personnalisé sous forme de paires clé-valeur.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="object">
  Détails de la requête à l’origine de la transaction.

  <Expandable title="propriétés de la requête" defaultOpen>
    <ResponseField name="geoip" type="dictionary">
      <Expandable title="propriétés de geoip" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      Le nom d’hôte utilisé pour le flux d’authentification.
    </ResponseField>

    <ResponseField name="ip" type="string">
      L’adresse IP à l’origine de la requête.
    </ResponseField>

    <ResponseField name="language" type="string" post={["optional"]}>
      La langue demandée par le navigateur.
    </ResponseField>

    <ResponseField name="method" type="string">
      La méthode HTTP utilisée pour la requête
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      La valeur de l’en-tête `User-Agent` reçue lors de l’initialisation de la transaction.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  Valeurs secrètes associées de manière sécurisée à cette Action.
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  Détails sur le tenant associé à la transaction en cours.

  <Expandable title="Propriétés d’event.tenant" defaultOpen>
    <ResponseField name="id" type="string">
      Le nom du tenant.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="object" post={["optional"]}>
  Détails de la transaction en cours.

  <Expandable title="propriétés de event.transaction" defaultOpen>
    <ResponseField name="correlation_id" type="string" post={["optional"]}>
      L’ID de corrélation peut être fourni dans la requête d’authentification initiale lorsque l’application redirige vers Universal Login. Vous pouvez utiliser cette valeur pour corréler les logs et les requêtes provenant du code de votre Action avec le flux utilisateur.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventuser">
  ## `event.user`
</div>

<ResponseField name="event.user" type="object">
  Objet décrivant l’utilisateur pour lequel la transaction en cours a été initiée.

  <Expandable title="propriétés de event.user" defaultOpen>
    <ResponseField name="email" type="string" post={["optional"]}>
      (unique) Adresse courriel de l’utilisateur.
    </ResponseField>

    <ResponseField name="email_verified" type="boolean" post={["optional"]}>
      Indique si l’utilisateur a vérifié son adresse courriel.
    </ResponseField>

    <ResponseField name="last_password_reset" type="string" post={["optional"]}>
      Horodatage indiquant la dernière réinitialisation ou modification du mot de passe de l’utilisateur. Ce champ n’existe pas lors de la création de l’utilisateur. Cette propriété est offerte uniquement pour les connexions à une base de données.
    </ResponseField>

    <ResponseField name="phone_number" type="string" post={["optional"]}>
      (unique) Numéro de téléphone de l’utilisateur.
    </ResponseField>

    <ResponseField name="phone_verified" type="boolean" post={["optional"]}>
      Indique si l’utilisateur a vérifié son numéro de téléphone.
    </ResponseField>

    <ResponseField name="user_id" type="string" post={["optional"]}>
      (unique) Identifiant unique de l’utilisateur.
    </ResponseField>

    <ResponseField name="username" type="string" post={["optional"]}>
      (unique) Nom d’utilisateur.
    </ResponseField>
  </Expandable>
</ResponseField>