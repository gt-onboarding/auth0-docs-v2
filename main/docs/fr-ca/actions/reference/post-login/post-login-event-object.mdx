---
#
# CE FICHIER EST GÉNÉRÉ AUTOMATIQUEMENT.
# NE LE MODIFIEZ PAS MANUELLEMENT.
#
description: Découvrez l’objet event du déclencheur Actions post-login, qui fournit des informations contextuelles sur l’exécution du déclencheur.
trigger: post-login
pageType: event
title: 'Objet event'
sidebarTitle: 'Objet event'
---

L’objet `event` du déclencheur Actions post-login fournit des informations contextuelles sur l’exécution du déclencheur.

```json Event Object
{
  "secrets": {},
  "client": {
    "client_id": "string",
    "metadata": {},
    "name": "string"
  },
  "connection": {
    "id": "string",
    "name": "string",
    "strategy": "string"
  },
  "request": {
    "body": {},
    "geoip": {},
    "ip": "string",
    "method": "string",
    "query": {}
  },
  "stats": {
    "logins_count": 0
  },
  "tenant": {
    "id": "string"
  },
  "user": {}
}
```

<div id="eventagent">
  ## `event.agent`
</div>

<ResponseField name="event.agent" type="object" post={["optional"]}>
  &#91;Accès anticipé&#93; Informations sur l’agent intervenant dans ce flux. Défini lorsque le client qui s’authentifie est lié à un agent et que les agents en tant que principaux sont activés dans le tenant; `undefined` sinon.

  <Expandable title="propriétés de event.agent" defaultOpen>
    <ResponseField name="agent_id" type="string">
      &#91;Accès anticipé&#93; L’identifiant stable de l’agent, précédé du préfixe `agt_` (par exemple, `agt_2hVk6JxPxbRgNZDKfJQqmn`).
    </ResponseField>

    <ResponseField name="agent_metadata" type="dictionary">
      &#91;Accès anticipé&#93; Métadonnées libres sous forme de paires clé-valeur associées à l’agent. Toujours définies lorsque `agent` est présent; objet vide lorsque l’agent ne possède aucune métadonnée.
    </ResponseField>

    <ResponseField name="name" type="string">
      &#91;Accès anticipé&#93; Le nom de l’agent, lisible par les humains.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventauthentication">
  ## `event.authentication`
</div>

<ResponseField name="event.authentication" type="object" post={["optional"]}>
  Détails sur les signaux d’authentification obtenus lors du processus de connexion.

  <Expandable title="Propriétés de event.authentication" defaultOpen>
    <ResponseField name="méthodes" type="array of objects">
      Contient les méthodes d’authentification qu’un utilisateur a utilisées au cours de sa session.
    </ResponseField>

    <ResponseField name="évaluationDesRisques" type="object" post={["optional"]}>
      Détails sur les évaluations des risques obtenues lors du flux de connexion ou de réinitialisation du mot de passe.

      <Expandable title="Propriétés de riskAssessment" defaultOpen>
        <ResponseField name="évaluations" type="object">
          <Expandable title="propriétés des évaluations" defaultOpen>
            <ResponseField name="AgentDetection" type="object" post={["optional"]}>
              Détermine si l’utilisateur se connecte en tant qu’agent connu.

              <Expandable title="propriétés d’AgentDetection" defaultOpen>
                <ResponseField name="code" type="string">
                  Valeurs autorisées : `unknown`, `verified_agent`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  Valeurs autorisées : `low`, `medium`, `high`, `neutral`
                </ResponseField>

                <ResponseField name="details" type="object" post={["optional"]}>
                  <Expandable title="propriétés des détails" defaultOpen>
                    <ResponseField name="provider" type="string" post={["optional"]} />
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="ImpossibleTravel" type="object" post={["optional"]}>
              Détermine si l’utilisateur se connecte depuis un lieu indiquant un déplacement impossible.

              <Expandable title="propriétés d’ImpossibleTravel" defaultOpen>
                <ResponseField name="code" type="string">
                  Valeurs autorisées : `minimal_travel_from_last_login`, `travel_from_last_login`, `substantial_travel_from_last_login`, `impossible_travel_from_last_login`, `invalid_travel`, `missing_geoip`, `anonymous_proxy`, `unknown_location`, `initial_login`, `location_history_not_found`, `assessment_not_available`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  Valeurs autorisées : `low`, `medium`, `high`, `neutral`
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="NewDevice" type="object" post={["optional"]}>
              Détermine si l’utilisateur se connecte depuis un appareil connu.

              <Expandable title="propriétés de NewDevice" defaultOpen>
                <ResponseField name="code" type="string">
                  Valeurs autorisées : `initial_login`, `assessment_not_available`, `match`, `partial_match`, `no_match`, `unknown_device`, `no_device_history`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  Valeurs autorisées : `low`, `medium`, `high`, `neutral`
                </ResponseField>

                <ResponseField name="details" type="object" post={["optional"]}>
                  <Expandable title="propriétés des détails" defaultOpen>
                    <ResponseField name="device" type="string" post={["optional"]}>
                      Valeurs autorisées : `unknown`, `known`
                    </ResponseField>

                    <ResponseField name="useragent" type="string" post={["optional"]}>
                      Valeurs autorisées : `unknown`, `known`
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="UntrustedIP" type="object" post={["optional"]}>
              Indique si l’adresse IP figure dans le dépôt d’Auth0 d’adresses IP à faible réputation.

              <Expandable title="propriétés d’UntrustedIP" defaultOpen>
                <ResponseField name="code" type="string">
                  Valeurs autorisées : `assessment_not_available`, `not_found_on_deny_list`, `found_on_deny_list`, `invalid_ip_address`
                </ResponseField>

                <ResponseField name="confidence" type="string">
                  Valeurs autorisées : `low`, `medium`, `high`, `neutral`
                </ResponseField>

                <ResponseField name="details" type="object" post={["optional"]}>
                  <Expandable title="propriétés des détails" defaultOpen>
                    <ResponseField name="category" type="string" post={["optional"]} />

                    <ResponseField name="ip" type="string" post={["optional"]}>
                      L’adresse IP d’origine de la requête.
                    </ResponseField>

                    <ResponseField name="matches" type="string" post={["optional"]} />

                    <ResponseField name="source" type="string" post={["optional"]} />
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="confiance" type="string">
          Score de risque global

          Valeurs permises : `low`, `medium`, `high`, `neutral`
        </ResponseField>

        <ResponseField name="supplémentaire" type="object" post={["optional"]}>
          &#91;Early Access&#93; Évaluation supplémentaire des risques.

          <Expandable title="propriétés supplémentaires" defaultOpen>
            <ResponseField name="akamai" type="dictionary" post={["optional"]}>
              <Expandable title="propriétés akamai" defaultOpen>
                <ResponseField name="akamaiBot" type="object" post={["optional"]}>
                  Les résultats de détection de bots transmis par Akamai Bot Manager.

                  <Expandable title="propriétés akamaiBot" defaultOpen>
                    <ResponseField name="type" type="string" post={["optional"]}>
                      Le type de résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="action" type="string" post={["optional"]}>
                      L’action associée aux résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botCategory" type="array of string" post={["optional"]}>
                      La catégorie de bots associée aux résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botScore" type="number" post={["optional"]}>
                      Le score de bot associé aux résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botScoreResponseSegment" type="string" post={["optional"]}>
                      Le segment de réponse du score de bot associé aux résultats d’Akamai Bot Manager.
                    </ResponseField>

                    <ResponseField name="botnetId" type="string" post={["optional"]}>
                      L’ID du réseau de bots associé aux résultats d’Akamai Bot Manager.
                    </ResponseField>
                  </Expandable>
                </ResponseField>

                <ResponseField name="akamaiUserRisk" type="object" post={["optional"]}>
                  Les résultats de détection des risques liés aux utilisateurs transmis par Akamai Account Protector.

                  <Expandable title="propriétés akamaiUserRisk" defaultOpen>
                    <ResponseField name="action" type="string" post={["optional"]}>
                      L’action de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="allow" type="number" post={["optional"]}>
                      Le statut d’autorisation de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="emailDomain" type="string" post={["optional"]}>
                      Le domaine de courriel de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="general" type="dictionary" post={["optional"]}>
                      Le niveau de risque général de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="ouid" type="string" post={["optional"]}>
                      L’OUID de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="requestid" type="string" post={["optional"]}>
                      L’ID de requête de l’utilisateur.
                    </ResponseField>

                    <ResponseField name="risk" type="dictionary" post={["optional"]}>
                      Le niveau de risque de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="score" type="number" post={["optional"]}>
                      Le score de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="status" type="number" post={["optional"]}>
                      Le statut de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="trust" type="dictionary" post={["optional"]}>
                      Le niveau de confiance de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>

                    <ResponseField name="username" type="string" post={["optional"]}>
                      Le nom d’utilisateur.
                    </ResponseField>

                    <ResponseField name="uuid" type="string" post={["optional"]}>
                      L’UUID de l’évaluation des risques utilisateur d’Akamai.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="version" type="string" />
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventauthorization">
  ## `event.authorization`
</div>

<ResponseField name="event.authorization" type="object" post={["optional"]}>
  Objet contenant des renseignements sur l’autorisation accordée à l’utilisateur qui ouvre une session.

  <Expandable title="Propriétés de event.authorization" defaultOpen>
    <ResponseField name="roles" type="array of string">
      Tableau contenant les noms des rôles attribués à un utilisateur.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventclient">
  ## `event.client`
</div>

<ResponseField name="event.client" type="object">
  Informations sur le client avec lequel cette transaction de connexion a été initiée.

  <Expandable title="propriétés de event.client" defaultOpen>
    <ResponseField name="client_id" type="string">
      L’ID client de l’application à laquelle l’utilisateur se connecte.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Objet permettant de stocker d’autres propriétés de l’application.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’application, tel qu’il est défini dans le Dashboard.
    </ResponseField>

    <ResponseField name="refresh_token" type="object" post={["optional"]}>
      &#91;Accès anticipé&#93; Objet contenant les propriétés de configuration du jeton d’actualisation.

      <Expandable title="propriétés de refresh_token" defaultOpen>
        <ResponseField name="policies" type="array of objects" post={["optional"]}>
          &#91;Accès anticipé&#93; Collection de politiques régissant l’échange de jetons d’actualisation multiressources (MRRT) et définissant comment les jetons d’actualisation peuvent être utilisés auprès de différents serveurs de ressources
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventconnection">
  ## `event.connection`
</div>

<ResponseField name="event.connection" type="object">
  Informations sur la connexion utilisée pour authentifier l’utilisateur.

  <Expandable title="propriétés de event.connection" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant unique de la connexion.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      Métadonnées associées à la connexion.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de la connexion utilisée pour authentifier l’utilisateur (par exemple, `twitter` ou `some-g-suite-domain`).
    </ResponseField>

    <ResponseField name="strategy" type="string">
      Le type de connexion. Pour les connexions sociales, `event.connection.strategy === event.connection.name`. Pour les connexions d’entreprise, la stratégie peut être `waad` (Windows Azure AD), `ad` (Active Directory/LDAP), `auth0` (connexions de base de données), etc.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventcustom_domain">
  ## `event.custom_domain`
</div>

<ResponseField name="event.custom_domain" type="object" post={["optional"]}>
  Détails sur le domaine personnalisé associé à la transaction en cours.

  <Expandable title="Propriétés de event.custom_domain" defaultOpen>
    <ResponseField name="domain" type="string">
      Le nom du domaine personnalisé.
    </ResponseField>

    <ResponseField name="domain_metadata" type="dictionary">
      Métadonnées du domaine personnalisé sous forme de paires clé-valeur.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventorganization">
  ## `event.organization`
</div>

<ResponseField name="event.organization" type="dictionary" post={["optional"]}>
  Détails sur l’organisation associée à la transaction en cours.

  <Expandable title="propriétés de event.organization" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant de l’organisation.
    </ResponseField>

    <ResponseField name="display_name" type="string">
      Le nom d’affichage de l’organisation.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Les métadonnées associées à l’organisation.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’organisation.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventprompt">
  ## `event.prompt`
</div>

<ResponseField name="event.prompt" type="object" post={["optional"]}>
  Données recueillies à partir des invites personnalisées affichées.

  <Expandable title="Propriétés de event.prompt" defaultOpen>
    <ResponseField name="id" type="string">
      L’ID de l’invite.
    </ResponseField>

    <ResponseField name="fields" type="dictionary" post={["optional"]}>
      Données des champs et des champs masqués.
    </ResponseField>

    <ResponseField name="vars" type="dictionary" post={["optional"]}>
      Données des variables partagées.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrefresh_token">
  ## `event.refresh_token`
</div>

<ResponseField name="event.refresh_token" type="object" post={["optional"]}>
  &#91;Clients Enterprise&#93; Le jeton d’actualisation actuel.

  <Expandable title="propriétés de event.refresh_token" defaultOpen>
    <ResponseField name="id" type="string">
      &#91;Clients Enterprise&#93; L’ID du jeton d’actualisation.
    </ResponseField>

    <ResponseField name="client_id" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; L’ID du client associé au jeton d’actualisation.
    </ResponseField>

    <ResponseField name="created_at" type="string">
      &#91;Clients Enterprise&#93; Horodatage de création du jeton d’actualisation.
    </ResponseField>

    <ResponseField name="device" type="object" post={["optional"]}>
      <Expandable title="propriétés de device" defaultOpen>
        <ResponseField name="initial_asn" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Premier numéro de système autonome associé à ce jeton d’actualisation.
        </ResponseField>

        <ResponseField name="initial_ip" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Première adresse IP associée à ce jeton d’actualisation.
        </ResponseField>

        <ResponseField name="initial_user_agent" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Premier agent utilisateur de l’appareil associé à ce jeton d’actualisation.
        </ResponseField>

        <ResponseField name="last_asn" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Dernier numéro de système autonome depuis lequel ce jeton d’actualisation a été échangé.
        </ResponseField>

        <ResponseField name="last_ip" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Dernière adresse IP depuis laquelle ce jeton d’actualisation a été échangé.
        </ResponseField>

        <ResponseField name="last_user_agent" type="string" post={["optional"]}>
          &#91;Clients Enterprise&#93; Dernier agent utilisateur de l’appareil depuis lequel ce jeton d’actualisation a été échangé.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="expires_at" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; Horodatage auquel le jeton d’actualisation expirera définitivement.
    </ResponseField>

    <ResponseField name="idle_expires_at" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; Horodatage auquel le jeton d’actualisation expirera en raison de l’inactivité.
    </ResponseField>

    <ResponseField name="last_exchanged_at" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; Horodatage du dernier échange réussi du jeton d’actualisation.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      Métadonnées du jeton d’actualisation
    </ResponseField>

    <ResponseField name="resource_servers" type="array of objects" post={["optional"]} />

    <ResponseField name="rotating" type="boolean" post={["optional"]}>
      &#91;Clients Enterprise&#93; Indique si le jeton d’actualisation est un jeton d’actualisation avec rotation.
    </ResponseField>

    <ResponseField name="session_id" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; L’ID de la session liée au jeton d’actualisation.
    </ResponseField>

    <ResponseField name="session_transfer" type="object" post={["optional"]}>
      &#91;Clients Enterprise&#93; Cet objet est défini lorsque la session est créée à partir d’un jeton de transfert de session (Native to Web SSO); sinon, il n’est pas défini.

      <Expandable title="propriétés de session_transfer" defaultOpen>
        <ResponseField name="parent_refresh_token" type="object" post={["optional"]}>
          &#91;Clients Enterprise&#93; Cet objet est défini lorsque le jeton d’actualisation est créé à partir d’une session initiée à la suite d’un transfert de session (Native to Web SSO); sinon, il n’est pas défini.

          <Expandable title="propriétés de parent_refresh_token" defaultOpen>
            <ResponseField name="id" type="string" post={["optional"]}>
              &#91;Clients Enterprise&#93; L’ID du jeton d’actualisation parent à partir duquel cette session ou ce jeton d’actualisation a été créé à la suite d’un transfert de session (Native to Web SSO).
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="user_id" type="string" post={["optional"]}>
      &#91;Clients Enterprise&#93; L’ID de l’utilisateur lié au jeton d’actualisation.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="object">
  Détails de la requête ayant initié la transaction.

  <Expandable title="propriétés de la requête" defaultOpen>
    <ResponseField name="asn" type="string" post={["optional"]}>
      Le numéro de système autonome (ASN) de l’agent utilisateur qui effectue la requête.
    </ResponseField>

    <ResponseField name="body" type="dictionary">
      Le corps de la requête POST. Ces données sont accessibles uniquement dans les flux de jeton d’actualisation et d’échange d’identifiants client, ainsi que dans l’Action Post Login.
    </ResponseField>

    <ResponseField name="geoip" type="dictionary">
      <Expandable title="propriétés de geoip" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      Le nom d’hôte utilisé pour le flux d’authentification.
    </ResponseField>

    <ResponseField name="ip" type="string">
      L’adresse IP d’origine de la requête.
    </ResponseField>

    <ResponseField name="language" type="string" post={["optional"]}>
      La langue demandée par le navigateur.
    </ResponseField>

    <ResponseField name="method" type="string">
      La méthode HTTP utilisée pour la requête
    </ResponseField>

    <ResponseField name="query" type="dictionary">
      Les paramètres de chaîne de requête envoyés avec la requête d’autorisation.
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      La valeur de l’en-tête `User-Agent` reçue lors de l’initialisation de la transaction.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventresource_server">
  ## `event.resource_server`
</div>

<ResponseField name="event.resource_server" type="object" post={["optional"]}>
  Détails sur le serveur de ressources auquel l’accès est demandé.

  <Expandable title="Propriétés de event.resource_server" defaultOpen>
    <ResponseField name="identifier" type="string">
      L’identifiant du serveur de ressources. Par exemple : `https://your-api.example.com`.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  Valeurs secrètes associées de manière sécurisée à cette Action.
</ResponseField>

<div id="eventsecurity_context">
  ## `event.security_context`
</div>

<ResponseField name="event.security_context" type="object" post={["optional"]}>
  Objet contenant des signatures d’empreinte. Il est disponible uniquement si le client utilise Cloudflare. L’empreinte JA3/JA4 peut être null ou vide dans certains cas, notamment pour les requêtes HTTP, puisque JA3 et JA4 sont calculées dans TLS. Elle peut également être vide lorsque le Worker envoie des requêtes dans la même zone ou vers une zone non proxifiée (ou vers un tiers).

  <Expandable title="Propriétés de event.security_context" defaultOpen>
    <ResponseField name="ja3" type="string" post={["optional"]}>
      Signature d’empreinte JA3. Elle est disponible uniquement si le client utilise une connexion TLS.
    </ResponseField>

    <ResponseField name="ja4" type="string" post={["optional"]}>
      Signature d’empreinte JA4. Elle est disponible uniquement si le client utilise une connexion TLS.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsession">
  ## `event.session`
</div>

<ResponseField name="event.session" type="object" post={["optional"]}>
  La session de connexion en cours.

  <Expandable title="Propriétés d’event.session" defaultOpen>
    <ResponseField name="id" type="string">
      L’ID de la session en cours.
    </ResponseField>

    <ResponseField name="actor" type="dictionary" post={["optional"]}>
      L’acteur des sessions établies à l’aide de jetons de transfert de session issus de l’échange de jetons personnalisé. Contient une propriété &#39;sub&#39; obligatoire et jusqu’à 5 propriétés supplémentaires définies par l’action d’échange de jetons personnalisé. L’acteur n’est défini que pour les sessions déléguées.

      <Expandable title="propriétés de l’acteur" defaultOpen>
        <ResponseField name="sub" type="string">
          L’identifiant du sujet de l’acteur. Un identifiant unique de l’entité qui agit dans ce rôle.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="authenticated_at" type="string" post={["optional"]}>
      &#91;Clients d’entreprise&#93; La date et l’heure de la dernière authentification de la session.
    </ResponseField>

    <ResponseField name="clients" type="array of objects" post={["optional"]}>
      &#91;Clients d’entreprise&#93; Liste des détails du client pour la session.
    </ResponseField>

    <ResponseField name="cookie" type="object" post={["optional"]}>
      &#91;Enterprise Customers&#93; Configuration du cookie de session, qui détermine la façon dont le cookie de session est géré par l’agent utilisateur.

      <Expandable title="propriétés du cookie" defaultOpen>
        <ResponseField name="mode" type="string">
          &#91;Enterprise Customers&#93; Le mode de persistance du cookie de session. S’il est défini sur &#39;non-persistent&#39; (éphère), le cookie est supprimé à la fermeture du navigateur. S’il est défini sur &#39;persistent&#39;, le cookie est conservé jusqu’à son expiration ou à sa suppression par l’utilisateur.

          Valeurs autorisées : `persistent`, `non-persistent`
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="created_at" type="string" post={["optional"]}>
      &#91;Enterprise Customers&#93; La date et l’heure de création de la session.
    </ResponseField>

    <ResponseField name="device" type="object" post={["optional"]}>
      &#91;Clients d’entreprise&#93; Métadonnées liées à l’appareil utilisé durant la session.

      <Expandable title="propriétés de l’appareil" defaultOpen>
        <ResponseField name="initial_asn" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Premier numéro de système autonome associé à cette session.
        </ResponseField>

        <ResponseField name="initial_ip" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Première adresse IP associée à cette session.
        </ResponseField>

        <ResponseField name="initial_user_agent" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Premier agent utilisateur de l’appareil associé à cette session.
        </ResponseField>

        <ResponseField name="last_asn" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Dernier numéro de système autonome utilisé par cet utilisateur pour se connecter.
        </ResponseField>

        <ResponseField name="last_ip" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Dernière adresse IP utilisée par cet utilisateur pour se connecter.
        </ResponseField>

        <ResponseField name="last_user_agent" type="string" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Dernier agent utilisateur de l’appareil utilisé par cet utilisateur pour se connecter.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="expire_le" type="string" post={["optional"]}>
      &#91;Enterprise Customers&#93; La date et l’heure d’expiration de la session.
    </ResponseField>

    <ResponseField name="expire_inactivité_à" type="string" post={["optional"]}>
      &#91;Enterprise Customers&#93; La date et l’heure auxquelles la session expirera en cas d’inactivité.
    </ResponseField>

    <ResponseField name="dernière_interaction_à" type="string" post={["optional"]}>
      &#91;Clients d’entreprise&#93; Date et heure de la dernière interaction réussie avec la session.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      &#91;Enterprise Customers&#93; &#91;Early Access&#93; Métadonnées de session
    </ResponseField>

    <ResponseField name="transfert_de_session" type="object" post={["optional"]}>
      &#91;Clients d’entreprise&#93; &#91;Accès anticipé&#93; Cet objet est défini lorsque la session est créée à partir d’un jeton de transfert de session (SSO Native to Web); sinon, il est indéfini.

      <Expandable title="propriétés de session_transfer" defaultOpen>
        <ResponseField name="parent_refresh_token" type="object" post={["optional"]}>
          &#91;Clients d’entreprise&#93; Cet objet est défini lorsque le jeton d’actualisation est créé à partir d’une session initiée à la suite d’un transfert de session (SSO Native to Web); sinon, il est indéfini.

          <Expandable title="propriétés de parent_refresh_token" defaultOpen>
            <ResponseField name="id" type="string" post={["optional"]}>
              &#91;Clients d’entreprise&#93; L’ID du jeton d’actualisation parent à partir duquel ce jeton de session ou d’actualisation a été créé à la suite d’un transfert de session (SSO Native to Web).
            </ResponseField>

            <ResponseField name="metadata" type="dictionary" post={["optional"]}>
              &#91;Clients d’entreprise&#93; Les métadonnées du jeton d’actualisation parent à partir duquel ce jeton de session ou d’actualisation a été créé à la suite d’un transfert de session (SSO Native to Web).
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="mis_à_jour_le" type="string" post={["optional"]}>
      &#91;Enterprise Customers&#93; La date et l’heure de la dernière mise à jour de la session.
    </ResponseField>

    <ResponseField name="user_id" type="string" post={["optional"]}>
      &#91;Clients d’entreprise&#93; ID de l’utilisateur pouvant être utilisé avec d’autres API.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsession_transfer_token">
  ## `event.session_transfer_token`
</div>

<ResponseField name="event.session_transfer_token" type="object" post={["optional"]}>
  &#91;Accès anticipé&#93; Détails du jeton de transfert de session actuel utilisé pour établir l’authentification unique (SSO) d’une application native vers une application Web.

  <Expandable title="propriétés de event.session_transfer_token" defaultOpen>
    <ResponseField name="client_id" type="string">
      &#91;Accès anticipé&#93; L’identifiant client de l’application qui a émis le jeton.
    </ResponseField>

    <ResponseField name="request" type="object">
      &#91;Accès anticipé&#93; Détails de la requête ayant émis le jeton.

      <Expandable title="propriétés de la requête" defaultOpen>
        <ResponseField name="asn" type="string" post={["optional"]}>
          &#91;Accès anticipé&#93; Le numéro de système autonome (ASN) associé à la requête ayant émis le jeton.
        </ResponseField>

        <ResponseField name="geoip" type="dictionary" post={["optional"]}>
          <Expandable title="propriétés de geoip" defaultOpen>
            <ResponseField name="cityName" type="string" post={["optional"]} />

            <ResponseField name="continentCode" type="string" post={["optional"]} />

            <ResponseField name="countryCode" type="string" post={["optional"]} />

            <ResponseField name="countryCode3" type="string" post={["optional"]} />

            <ResponseField name="countryName" type="string" post={["optional"]} />

            <ResponseField name="latitude" type="number" post={["optional"]} />

            <ResponseField name="longitude" type="number" post={["optional"]} />

            <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

            <ResponseField name="subdivisionName" type="string" post={["optional"]} />

            <ResponseField name="timeZone" type="string" post={["optional"]} />
          </Expandable>
        </ResponseField>

        <ResponseField name="ip" type="string">
          &#91;Accès anticipé&#93; L’adresse IP associée à la requête ayant émis le jeton.
        </ResponseField>

        <ResponseField name="user_agent" type="string" post={["optional"]}>
          &#91;Accès anticipé&#93; La chaîne User-Agent de l’appareil ayant émis le jeton.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="scope" type="array of string">
      &#91;Accès anticipé&#93; Les portées demandées lors de l’émission du jeton.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventstats">
  ## `event.stats`
</div>

<ResponseField name="event.stats" type="object">
  Statistiques de connexion de l’utilisateur actuel.

  <Expandable title="propriétés d’event.stats" defaultOpen>
    <ResponseField name="logins_count" type="number">
      Le nombre de fois que cet utilisateur s’est connecté.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  Détails sur le tenant associé à la transaction en cours.

  <Expandable title="propriétés de event.tenant" defaultOpen>
    <ResponseField name="id" type="string">
      Le nom du tenant.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="dictionary" post={["optional"]}>
  Détails sur la transaction en cours.

  <Expandable title="Propriétés de event.transaction" defaultOpen>
    <ResponseField name="id" type="string" post={["optional"]}>
      Identifiant unique de la transaction. Renseigné pour tous les flux de connexion par navigateur.
    </ResponseField>

    <ResponseField name="acr_values" type="array of string" post={["optional"]}>
      Toutes les valeurs acr&#95; fournies dans la requête d’authentification d’origine.
    </ResponseField>

    <ResponseField name="liaison_id" type="string" post={["optional"]}>
      ID de liaison dynamique permettant aux développeurs de référencer cette transaction.
    </ResponseField>

    <ResponseField name="paramètres régionaux" type="string" post={["optional"]}>
      Les paramètres régionaux à utiliser pour cette transaction, déterminés en comparant les langues demandées par le navigateur aux paramètres linguistiques du tenant.
    </ResponseField>

    <ResponseField name="login_hint" type="string" post={["optional"]}>
      Indication destinée au Authorization Server concernant l’identifiant de connexion que l’utilisateur final pourrait utiliser pour se connecter, au besoin.
    </ResponseField>

    <ResponseField name="invite" type="array of string" post={["optional"]}>
      Liste d’instructions indiquant si l’utilisateur peut être invité à se réauthentifier et à consentir.
    </ResponseField>

    <ResponseField name="protocole" type="string" post={["optional"]}>
      Valeurs autorisées : `oidc-basic-profile`, `oidc-ciba`, `oidc-ciba-web-link`, `oidc-implicit-profile`, `oauth2-device-code`, `oauth2-resource-owner`, `oauth2-resource-owner-jwt-bearer`, `oauth2-password`, `oauth2-webauthn`, `oauth2-access-token`, `oauth2-refresh-token`, `oauth2-token-exchange`, `oidc-hybrid-profile`, `samlp`, `wsfed`, `wstrust-usernamemixed`
    </ResponseField>

    <ResponseField name="redirect_uri" type="string" post={["optional"]}>
      L’URL vers laquelle Auth0 redirigera le navigateur une fois la transaction terminée.
    </ResponseField>

    <ResponseField name="requested_authorization_details" type="array of objects" post={["optional"]}>
      Les détails d’une requête d’autorisation enrichie, conformément à la section 2 de la spécification Rich Authorization Requests [https://datatracker.ietf.org/doc/html/draft-ietf-oauth-rar#section-2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-rar#section-2).
    </ResponseField>

    <ResponseField name="requested_scopes" type="array of string" post={["optional"]}>
      Les scopes demandés (s’il y a lieu) au début de ce flux d’authentification.
    </ResponseField>

    <ResponseField name="mode_de_réponse" type="string" post={["optional"]}>
      Informe le serveur d’autorisation du mécanisme à utiliser pour renvoyer les paramètres depuis le point de terminaison d’autorisation.

      Valeurs autorisées : `query`, `fragment`, `form_post`, `web_message`
    </ResponseField>

    <ResponseField name="type_de_réponse" type="array of string" post={["optional"]}>
      Indique le type d’identifiant d’authentification qu’Auth0 retournera.
    </ResponseField>

    <ResponseField name="state" type="string" post={["optional"]}>
      Chaîne alphanumérique opaque et arbitraire que votre application ajoute à la requête initiale et qu’Auth0 inclut lors de la redirection vers votre application.
    </ResponseField>

    <ResponseField name="ui_locales" type="array of string" post={["optional"]}>
      Les ui&#95;locales fournis dans la requête d’authentification initiale.
    </ResponseField>

    <ResponseField name="actor" type="dictionary" post={["optional"]}>
      L’acteur dans une demande d’échange de jetons.

      <Expandable title="propriétés de l’acteur" defaultOpen>
        <ResponseField name="act" type="dictionary" post={["optional"]}>
          L’acteur suivant dans la chaîne de délégation, indiquant que cet acteur effectue la demande au nom d’un autre principal.

          <Expandable title="propriétés d’act" defaultOpen>
            <ResponseField name="act" type="dictionary" post={["optional"]}>
              L’acteur suivant dans la chaîne de délégation, indiquant que cet acteur effectue la demande au nom d’un autre principal.

              <Expandable title="propriétés d’act" defaultOpen>
                <ResponseField name="act" type="dictionary" post={["optional"]}>
                  L’acteur suivant dans la chaîne de délégation, indiquant que cet acteur effectue la demande au nom d’un autre principal.

                  <Expandable title="propriétés d’act" defaultOpen>
                    <ResponseField name="act" type="dictionary" post={["optional"]}>
                      L’acteur suivant dans la chaîne de délégation, indiquant que cet acteur effectue la demande au nom d’un autre principal.
                    </ResponseField>

                    <ResponseField name="sub" type="string">
                      L’identifiant du sujet de l’acteur. Un identifiant unique de l’entité agissant dans ce rôle.
                    </ResponseField>
                  </Expandable>
                </ResponseField>

                <ResponseField name="sub" type="string">
                  L’identifiant du sujet de l’acteur. Un identifiant unique de l’entité agissant dans ce rôle.
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="sub" type="string">
              L’identifiant du sujet de l’acteur. Un identifiant unique de l’entité agissant dans ce rôle.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="sub" type="string">
          L’identifiant du sujet de l’acteur. Un identifiant unique de l’entité agissant dans ce rôle.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="actor_token_type" type="string" post={["optional"]}>
      Le type du jeton d’acteur dans une demande d’échange de jetons.
    </ResponseField>

    <ResponseField name="correlation_id" type="string" post={["optional"]}>
      L’ID de corrélation peut être fourni dans la requête d’authentification initiale lorsque l’application redirige vers Universal Login. Vous pouvez utiliser cette valeur pour établir une corrélation entre les journaux et les requêtes issus du code de votre Action et le parcours utilisateur.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Un objet contenant des données partagées entre les Actions personnalisées pendant la durée d’une transaction.
    </ResponseField>

    <ResponseField name="subject_token_type" type="string" post={["optional"]}>
      Le type du jeton du sujet dans une requête d’échange de jetons.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventuser">
  ## `event.user`
</div>

<ResponseField name="event.user" type="dictionary">
  Objet décrivant l’utilisateur au nom duquel la transaction en cours a été initiée.

  <Expandable title="propriétés de event.user" defaultOpen>
    <ResponseField name="app_metadata" type="dictionary">
      Champs personnalisés contenant des informations sur un utilisateur qui influent sur son accès, comme son plan de soutien, ses rôles de sécurité ou ses groupes de contrôle d’accès.
    </ResponseField>

    <ResponseField name="created_at" type="string">
      Horodatage indiquant la date de création initiale du profil utilisateur.
    </ResponseField>

    <ResponseField name="email" type="string" post={["optional"]}>
      (unique) Adresse courriel de l’utilisateur.
    </ResponseField>

    <ResponseField name="email_verified" type="boolean">
      Indique si l’utilisateur a vérifié son adresse courriel.
    </ResponseField>

    <ResponseField name="family_name" type="string" post={["optional"]}>
      Nom de famille de l’utilisateur.
    </ResponseField>

    <ResponseField name="given_name" type="string" post={["optional"]}>
      Prénom de l’utilisateur.
    </ResponseField>

    <ResponseField name="last_password_reset" type="string" post={["optional"]}>
      Horodatage indiquant la dernière réinitialisation ou modification du mot de passe de l’utilisateur. Ce champ n’existe pas lors de la création de l’utilisateur. Cette propriété est disponible uniquement pour les connexions de base de données.
    </ResponseField>

    <ResponseField name="name" type="string" post={["optional"]}>
      Nom complet de l’utilisateur.
    </ResponseField>

    <ResponseField name="nickname" type="string" post={["optional"]}>
      Pseudonyme de l’utilisateur.
    </ResponseField>

    <ResponseField name="phone_number" type="string" post={["optional"]}>
      Numéro de téléphone de l’utilisateur.
    </ResponseField>

    <ResponseField name="phone_verified" type="boolean" post={["optional"]}>
      Indique si l’utilisateur a vérifié son numéro de téléphone.
    </ResponseField>

    <ResponseField name="picture" type="string" post={["optional"]}>
      URL pointant vers la [photo de profil de l’utilisateur](https://auth0.com/docs/users/change-user-picture).
    </ResponseField>

    <ResponseField name="updated_at" type="string">
      Horodatage indiquant la dernière mise à jour ou modification du profil de l’utilisateur.
    </ResponseField>

    <ResponseField name="user_id" type="string">
      (unique) Identifiant unique de l’utilisateur.
    </ResponseField>

    <ResponseField name="user_metadata" type="dictionary">
      Champs personnalisés contenant des informations sur un utilisateur qui n’ont aucune incidence sur ses droits d’accès, comme son adresse professionnelle, son adresse résidentielle ou ses préférences.
    </ResponseField>

    <ResponseField name="username" type="string" post={["optional"]}>
      (unique) Nom d’utilisateur de l’utilisateur.
    </ResponseField>

    <ResponseField name="enrolledFactors" type="array of objects" post={["optional"]}>
      Tableau des facteurs d’authentification auxquels l’utilisateur est inscrit.
    </ResponseField>

    <ResponseField name="multifactor" type="array of string" post={["optional"]}>
      Liste des fournisseurs d’authentification multifacteur (MFA) auprès desquels l’utilisateur est inscrit. Ce tableau est mis à jour lorsque l’utilisateur s’inscrit à la MFA et lorsqu’un administrateur réinitialise les inscriptions MFA d’un utilisateur.
    </ResponseField>

    <ResponseField name="identities" type="array of objects">
      Contient les informations récupérées auprès du fournisseur d’identité auprès duquel l’utilisateur s’est initialement authentifié. Les utilisateurs peuvent également lier leur profil à plusieurs fournisseurs d’identité; ces identités apparaîtront alors aussi dans ce tableau. Le contenu d’un objet de fournisseur d’identité varie selon le fournisseur.
    </ResponseField>
  </Expandable>
</ResponseField>