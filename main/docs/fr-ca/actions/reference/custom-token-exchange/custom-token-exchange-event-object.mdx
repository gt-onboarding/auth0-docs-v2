---
#
# CE FICHIER EST GÉNÉRÉ AUTOMATIQUEMENT.
# NE LE MODIFIEZ PAS MANUELLEMENT.
#
description: Découvrez l’objet d’événement du déclencheur Actions custom-token-exchange, qui fournit des renseignements contextuels sur l’exécution du déclencheur.
trigger: custom-token-exchange
pageType: event
title: 'Objet d’événement'
sidebarTitle: 'Objet d’événement'
---

L’objet `event` du déclencheur Actions custom-token-exchange fournit des renseignements contextuels sur l’exécution du déclencheur.

```json Event Object
{
  "secrets": {},
  "client": {
    "client_id": "string",
    "metadata": {},
    "name": "string"
  },
  "request": {
    "body": {},
    "geoip": {},
    "ip": "string",
    "method": "string"
  },
  "resource_server": {
    "identifier": "string"
  },
  "tenant": {
    "id": "string"
  },
  "transaction": {
    "requested_scopes": [],
    "requested_token_type": "string",
    "subject_token": "string",
    "subject_token_type": "string"
  }
}
```

<div id="eventclient">
  ## `event.client`
</div>

<ResponseField name="event.client" type="object">
  Renseignements sur le client à l’origine de cette transaction.

  <Expandable title="propriétés de event.client" defaultOpen>
    <ResponseField name="client_id" type="string">
      L’ID client de l’application à laquelle l’utilisateur se connecte.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Objet contenant d’autres propriétés de l’application.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’application (tel qu’il est défini dans le Dashboard).
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventorganization">
  ## `event.organization`
</div>

<ResponseField name="event.organization" type="dictionary" post={["optional"]}>
  Détails sur l’organisation associée à la transaction en cours.

  <Expandable title="propriétés de event.organization" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant de l’organisation.
    </ResponseField>

    <ResponseField name="display_name" type="string">
      Le nom convivial de l’organisation.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Les métadonnées associées à l’organisation.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’organisation.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="object">
  Détails de la requête à l’origine de la transaction.

  <Expandable title="propriétés de la requête" defaultOpen>
    <ResponseField name="body" type="dictionary">
      Le corps de la requête POST. Ces données sont disponibles uniquement durant les flows de refresh token et d’échange d’identifiants client, ainsi que dans l’Action PreUserRegistration.
    </ResponseField>

    <ResponseField name="geoip" type="dictionary">
      <Expandable title="propriétés de geoip" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      Le nom d’hôte utilisé pour le flow d’authentification.
    </ResponseField>

    <ResponseField name="ip" type="string">
      L’adresse IP à l’origine de la requête.
    </ResponseField>

    <ResponseField name="language" type="string" post={["optional"]}>
      La langue demandée par le navigateur.
    </ResponseField>

    <ResponseField name="method" type="string">
      La méthode HTTP utilisée pour la requête
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      La valeur de l’en-tête `User-Agent` reçue lors de l’initialisation de la transaction.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventresource_server">
  ## `event.resource_server`
</div>

<ResponseField name="event.resource_server" type="object">
  Détails sur le serveur de ressources auquel l’accès est demandé.

  <Expandable title="propriétés de event.resource_server" defaultOpen>
    <ResponseField name="identifier" type="string">
      L’identifiant du serveur de ressources. Par exemple : `https://your-api.example.com`.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  Valeurs secrètes associées de façon sécurisée à cette Action.
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  Renseignements sur le tenant associé à la transaction actuelle.

  <Expandable title="propriétés de event.tenant" defaultOpen>
    <ResponseField name="id" type="string">
      Le nom du tenant.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="object">
  Détails sur la transaction d&#39;échange de jeton personnalisé en cours.

  <Expandable title="Propriétés d’event.transaction" defaultOpen>
    <ResponseField name="actor_token" type="string" post={["optional"]}>
      Le jeton d’acteur fourni dans la request d’échange de jetons.
    </ResponseField>

    <ResponseField name="actor_token_type" type="string" post={["optional"]}>
      Le type du jeton d’acteur fourni dans la demande d’échange de jeton.
    </ResponseField>

    <ResponseField name="actor_token_user" type="dictionary" post={["optional"]}>
      L’utilisateur représenté par le jeton d’acteur. Cette valeur n’est présente que si actor&#95;token&#95;type est urn:ietf:params:oauth:token-type:id&#95;token et que le jeton d’acteur fourni dans la demande d’échange de jetons est un jeton ID valide généré par Auth0.

      <Expandable title="propriétés de actor_token_user" defaultOpen>
        <ResponseField name="app_metadata" type="dictionary">
          Champs personnalisés qui stockent des renseignements sur un utilisateur et influencent son accès, comme le plan de soutien, les rôles de sécurité ou les groupes de contrôle d’accès.
        </ResponseField>

        <ResponseField name="created_at" type="string">
          Horodatage indiquant quand le profil utilisateur a été créé.
        </ResponseField>

        <ResponseField name="email" type="string" post={["optional"]}>
          (unique) Adresse courriel de l’utilisateur.
        </ResponseField>

        <ResponseField name="email_verified" type="boolean">
          Indique si l’utilisateur a vérifié son adresse courriel.
        </ResponseField>

        <ResponseField name="family_name" type="string" post={["optional"]}>
          Nom de famille de l’utilisateur.
        </ResponseField>

        <ResponseField name="given_name" type="string" post={["optional"]}>
          Prénom de l’utilisateur.
        </ResponseField>

        <ResponseField name="last_password_reset" type="string" post={["optional"]}>
          Horodatage indiquant la dernière réinitialisation ou modification du mot de passe de l’utilisateur. Ce champ n’existe pas lors de la création de l’utilisateur. Cette propriété est offerte uniquement pour les connexions Database.
        </ResponseField>

        <ResponseField name="name" type="string" post={["optional"]}>
          Nom complet de l’utilisateur.
        </ResponseField>

        <ResponseField name="nickname" type="string" post={["optional"]}>
          Surnom de l’utilisateur.
        </ResponseField>

        <ResponseField name="phone_number" type="string" post={["optional"]}>
          Numéro de téléphone de l’utilisateur.
        </ResponseField>

        <ResponseField name="phone_verified" type="boolean" post={["optional"]}>
          Indique si l’utilisateur a vérifié son numéro de téléphone.
        </ResponseField>

        <ResponseField name="picture" type="string" post={["optional"]}>
          URL menant à la [photo de profil de l’utilisateur](https://auth0.com/docs/users/change-user-picture).
        </ResponseField>

        <ResponseField name="updated_at" type="string">
          Horodatage indiquant la dernière mise à jour ou modification du profil de l’utilisateur.
        </ResponseField>

        <ResponseField name="user_id" type="string">
          (unique) Identifiant unique de l’utilisateur.
        </ResponseField>

        <ResponseField name="user_metadata" type="dictionary">
          Champs personnalisés qui stockent des renseignements sur un utilisateur sans incidence sur les ressources auxquelles il peut accéder, comme l’adresse professionnelle, l’adresse domiciliaire ou les préférences de l’utilisateur.
        </ResponseField>

        <ResponseField name="username" type="string" post={["optional"]}>
          (unique) Nom d’utilisateur de l’utilisateur.
        </ResponseField>

        <ResponseField name="enrolledFactors" type="array of objects" post={["optional"]}>
          Tableau des facteurs d’authentification auxquels l’utilisateur s’est inscrit.
        </ResponseField>

        <ResponseField name="multifactor" type="array of string" post={["optional"]}>
          Liste des fournisseurs d’authentification multifacteur (MFA) auprès desquels l’utilisateur est inscrit. Ce tableau est mis à jour lorsque l’utilisateur s’inscrit à la MFA et lorsqu’un administrateur réinitialise les inscriptions MFA d’un utilisateur.
        </ResponseField>

        <ResponseField name="identities" type="array of objects">
          Contient des renseignements récupérés auprès du fournisseur d’identité auprès duquel l’utilisateur s’est initialement authentifié. Les utilisateurs peuvent aussi associer leur profil à plusieurs fournisseurs d’identité; ces identités apparaîtront alors également dans ce tableau. Le contenu d’un objet de fournisseur d’identité varie selon le fournisseur.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="scopes_demandés" type="array of string">
      Les scopes demandés (le cas échéant) fournis dans la demande d’échange de token.
    </ResponseField>

    <ResponseField name="requested_token_type" type="string">
      Le type de token qu’Auth0 doit générer. Par exemple : urn:ietf:params:oauth:token-type:access&#95;token.
    </ResponseField>

    <ResponseField name="subject_token" type="string">
      Le jeton du sujet fourni dans la requête d’échange de jetons.
    </ResponseField>

    <ResponseField name="subject_token_type" type="string">
      Le type de subject&#95;token fourni dans la requête d’échange de token.
    </ResponseField>
  </Expandable>
</ResponseField>