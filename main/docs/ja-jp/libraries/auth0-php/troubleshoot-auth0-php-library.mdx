---
description: PHP アプリケーションでよくある問題のトラブルシューティング。
title: 'Auth0-PHP 連携のトラブルシューティング'
---

以下は、Auth0 PHP ライブラリの使用時に発生する可能性がある問題と、その対処方法の一覧です。

**ログインしようとすると &quot;Invalid State&quot; 例外が発生します。**

セキュリティを強化するため、[state のバリデーション](/docs/ja-jp/secure/attack-protection/state-parameters) が追加されました。デフォルトではセッションストレージが使用され、`Auth0::login()` と、コールバック内で `Auth0::exchange()` を呼び出すメソッドを組み合わせて使用している場合は、自動的に処理されます。

ユーザーにこのエラーが発生する場合:

* アプリケーションが誤って `Auth0::login()` を複数回呼び出していないことを確認してください。複数回呼び出すと、エンドユーザーのデバイスに保存された state が無効になる可能性があります。
* エンドユーザーがデバイスで最新のブラウザーを使用しており、Cookie をブロックしていないことを確認してください。

**Windows で** **`curl error 60: SSL certificate problem: self-signed certificate in certificate chain`** **が発生します**

これは、**Windows OS** 上の最新の PHP バージョンでよく見られる問題です (Windows と OpenSSL CA データベースの非互換性に関連しています) 。

1. この CA データベース `https://curl.haxx.se/ca/cacert.pem` を `c:/cacert.pem` にダウンロードします。
2. php.ini を編集し、`openssl.cafile=c:/cacert.pem` を追加します。 (ダウンロードしたファイルを指定する必要があります。)

**ホストで Composer を使用できません**

PHP SDK では、dependencies (外部 PHP ライブラリ) を管理するために Composer が必要です。ホストで Composer をグローバルにインストールできない場合でも、ユーザーのシェルアカウントで実行するためにローカルにインストールできます。手順は Composer の Web サイトにあります: [https://getcomposer.org/doc/00-intro.md#locally](https://getcomposer.org/doc/00-intro.md#locally)y&gt;

<div id="learn-more">
  ## 詳細情報
</div>

* [Auth0-PHP を使い始める](/docs/ja-jp/libraries/auth0-php)
* [Login、Logout、ユーザープロファイルを管理する](/docs/ja-jp/libraries/auth0-php/auth0-php-basic-use)
* [Authentication API を使用する](/docs/ja-jp/libraries/auth0-php/using-the-authentication-api-with-auth0-php)
* [Management API を使用する](/docs/ja-jp/libraries/auth0-php/using-the-management-api-with-auth0-php)
* [JWT (JSON Web Tokens) を検証する](/docs/ja-jp/libraries/auth0-php/validating-jwts-with-auth0-php)