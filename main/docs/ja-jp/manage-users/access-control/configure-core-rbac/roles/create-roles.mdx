---
description: Auth0 Dashboard または Management API を使用してロールを作成する方法を説明します。
title: ロールの作成
---

import {AuthCodeBlock} from "/snippets/ja-jp/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/ja-jp/AuthCodeGroup.jsx";

Auth0 Dashboard または <Tooltip tip="Management API: 顧客が管理タスクを実行できるようにする製品。" cta="用語集を見る" href="/docs/ja-jp/glossary?term=Management+API">Management API</Tooltip> を使用して、[ロール](/docs/ja-jp/manage-users/access-control/rbac)を作成できます。ロールは API Authorization Core の機能セットと連携して機能します。

Auth0 では、2 種類のロールを作成できます。

* テナントロール: テナント全体に適用され、任意のユーザーに割り当てることができます。
* 組織 ロール: 特定の 組織 にスコープが設定されます。ユーザーは、所属する 組織 ごとに異なる 組織 ロールを持つことができます。詳細については、[組織 Roles](/docs/ja-jp/manage-users/organizations/organization-roles)を参照してください。

<ReleaseStageNotice feature="組織 Roles" stage="ea" terms="true" contact="Auth0 Support" />

<div id="prerequisites">
  ## 前提条件
</div>

* ロールベースのアクセス制御 (RBAC) を正しく機能させるには、Auth0 Dashboard または Management API を使用して、API で RBAC を有効にする必要があります。Authorization Core の機能は Authorization Extension とは異なります。比較については、[Authorization Core と Authorization Extension の比較](/docs/ja-jp/manage-users/access-control/authorization-core-vs-authorization-extension)をご覧ください。
* <Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 のメイン製品です。" cta="用語集を見る" href="/docs/ja-jp/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip> で [API を設定](/docs/ja-jp/get-started/auth0-overview/set-up-apis) します。
* 権限は事前定義された値から取得されます。権限の一覧が空の場合は、API に[権限を追加](/docs/ja-jp/get-started/apis/add-api-permissions)する必要があります。

<div id="create-a-tenant-role">
  ## テナント ロールを作成する
</div>

テナント ロールは テナント 全体に適用され、組織のメンバーシップにかかわらず、任意のユーザーに割り当てることができます。

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. [Dashboard &gt; User Management &gt; Roles](https://manage.auth0.com/#/roles)に移動し、**ロールの作成**を選択します。
    2. ロールに名前を付け、説明を追加してから、**Create**を選択します。
  </Tab>

  <Tab title="Management API">
    [ロールの作成エンドポイント](/docs/ja-jp/api/management/v2#!/Roles/post_roles)に`POST`呼び出しを行います。`MGMT_API_ACCESS_TOKEN`、`ROLE_NAME`、`ROLE_DESC`のプレースホルダー値は、それぞれManagement APIの<Tooltip tip="アクセストークン：APIにアクセスするために使用される、不透明な文字列またはJWT形式の認可資格情報。" cta="用語集を表示" href="/docs/ja-jp/glossary?term=access+token">アクセストークン</Tooltip>、ロール名、ロールの説明に必ず置き換えてください。

    <AuthCodeGroup>
      ```bash cURL
      curl --request POST \
        --url 'https://{yourDomain}/api/v2/roles' \
        --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN' \
        --header 'cache-control: no-cache' \
        --header 'content-type: application/json' \
        --data '{ "name": "ROLE_NAME", "description": "ROLE_DESC" }'
      ```

      ```csharp C#
      var client = new RestClient("https://{yourDomain}/api/v2/roles");
      var request = new RestRequest(Method.POST);
      request.AddHeader("content-type", "application/json");
      request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
      request.AddHeader("cache-control", "no-cache");
      request.AddParameter("application/json", "{ "name": "ROLE_NAME", "description": "ROLE_DESC" }", ParameterType.RequestBody);
      IRestResponse response = client.Execute(request);
      ```

      ```go Go
      package main

      import (
      	"fmt"
      	"strings"
      	"net/http"
      	"io/ioutil"
      )

      func main() {

      	url := "https://{yourDomain}/api/v2/roles"

      	payload := strings.NewReader("{ "name": "ROLE_NAME", "description": "ROLE_DESC" }")

      	req, _ := http.NewRequest("POST", url, payload)

      	req.Header.Add("content-type", "application/json")
      	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
      	req.Header.Add("cache-control", "no-cache")

      	res, _ := http.DefaultClient.Do(req)

      	defer res.Body.Close()
      	body, _ := ioutil.ReadAll(res.Body)

      	fmt.Println(res)
      	fmt.Println(string(body))

      }
      ```

      ```java Java
      HttpResponse<String> response = Unirest.post("https://{yourDomain}/api/v2/roles")
        .header("content-type", "application/json")
        .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
        .header("cache-control", "no-cache")
        .body("{ "name": "ROLE_NAME", "description": "ROLE_DESC" }")
        .asString();
      ```

      ```javascript Node.JS
      var axios = require("axios").default;

      var options = {
        method: 'POST',
        url: 'https://{yourDomain}/api/v2/roles',
        headers: {
          'content-type': 'application/json',
          authorization: 'Bearer MGMT_API_ACCESS_TOKEN',
          'cache-control': 'no-cache'
        },
        data: {name: 'ROLE_NAME', description: 'ROLE_DESC'}
      };

      axios.request(options).then(function (response) {
        console.log(response.data);
      }).catch(function (error) {
        console.error(error);
      });
      ```

      ```php PHP
      $curl = curl_init();

      curl_setopt_array($curl, [
        CURLOPT_URL => "https://{yourDomain}/api/v2/roles",
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_ENCODING => "",
        CURLOPT_MAXREDIRS => 10,
        CURLOPT_TIMEOUT => 30,
        CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
        CURLOPT_CUSTOMREQUEST => "POST",
        CURLOPT_POSTFIELDS => "{ "name": "ROLE_NAME", "description": "ROLE_DESC" }",
        CURLOPT_HTTPHEADER => [
          "authorization: Bearer MGMT_API_ACCESS_TOKEN",
          "cache-control: no-cache",
          "content-type: application/json"
        ],
      ]);

      $response = curl_exec($curl);
      $err = curl_error($curl);

      curl_close($curl);

      if ($err) {
        echo "cURL Error #:" . $err;
      } else {
        echo $response;
      }
      ```

      ```python Python
      import http.client

      conn = http.client.HTTPSConnection("")

      payload = "{ "name": "ROLE_NAME", "description": "ROLE_DESC" }"

      headers = {
          'content-type': "application/json",
          'authorization': "Bearer MGMT_API_ACCESS_TOKEN",
          'cache-control': "no-cache"
          }

      conn.request("POST", "/{yourDomain}/api/v2/roles", payload, headers)

      res = conn.getresponse()
      data = res.read()

      print(data.decode("utf-8"))
      ```

      ```ruby Ruby
      require 'uri'
      require 'net/http'
      require 'openssl'

      url = URI("https://{yourDomain}/api/v2/roles")

      http = Net::HTTP.new(url.host, url.port)
      http.use_ssl = true
      http.verify_mode = OpenSSL::SSL::VERIFY_PEER

      request = Net::HTTP::Post.new(url)
      request["content-type"] = 'application/json'
      request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'
      request["cache-control"] = 'no-cache'
      request.body = "{ "name": "ROLE_NAME", "description": "ROLE_DESC" }"

      response = http.request(request)
      puts response.read_body
      ```
    </AuthCodeGroup>

    | **値**                   | **説明**                                                                                                |
    | ----------------------- | ----------------------------------------------------------------------------------------------------- |
    | `MGMT_API_ACCESS_TOKEN` | [Management API 用アクセストークン](https://auth0.com/docs/api/management/v2/tokens) (scope: `create:roles`) 。 |
    | `ROLE_NAME`             | 作成するroleの名前。                                                                                          |
    | `ROLE_DESC`             | roleのわかりやすい説明。                                                                                        |
  </Tab>
</Tabs>

<div id="create-an-organization-role">
  ## 組織ロールを作成する
</div>

組織ロールは、組織の管理画面から作成します。テナント管理者は、組織ごとに最大350個の組織ロールを作成できます。

<Tabs>
  <Tab title="Auth0 Dashboard">
    1. [Auth0 Dashboard &gt; 組織](https://manage.auth0.com/#/organizations)に移動し、組織を選択します。
    2. **ロール**タブを選択し、**ロールの作成**を選択します。
    3. ロール名と説明を入力し、**Create**を選択します。
  </Tab>

  <Tab title="Management API">
    [Create Role endpoint](/docs/ja-jp/api/management/v2#!/Roles/post_roles)に`POST`リクエストを送信します。`type`を`"organization"`に設定し、`owner_id`に組織IDを設定します。`MGMT_API_ACCESS_TOKEN`、`ROLE_NAME`、`ROLE_DESC`、`ORG_ID`のプレースホルダー値は、それぞれManagement APIの<Tooltip tip="アクセストークン: APIへのアクセスに使用する、不透明な文字列またはJWT形式のAuthorization認証情報。" cta="用語集を見る" href="/docs/ja-jp/glossary?term=access+token">アクセストークン</Tooltip>、ロール名、ロールの説明、組織IDに必ず置き換えてください。

    ```bash cURL
    curl --request POST \
      --url 'https://{yourDomain}/api/v2/roles' \
      --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN' \
      --header 'cache-control: no-cache' \
      --header 'content-type: application/json' \
      --data '{ "name": "ROLE_NAME", "description": "ROLE_DESC", "type": "organization", "owner_id": "ORG_ID" }'
    ```

    | **値**                   | **説明**                                                                          |
    | ----------------------- | ------------------------------------------------------------------------------- |
    | `MGMT_API_ACCESS_TOKEN` | スコープ`create:roles`を持つ[Management API用アクセストークン](/docs/ja-jp/api/management/v2/tokens)。 |
    | `ROLE_NAME`             | 作成する組織ロールの名前。組織内で一意である必要があります。                              |
    | `ROLE_DESC`             | ロールのわかりやすい説明。                                                                   |
    | `ORG_ID`                | ロールを作成する組織のID。                                                        |

    レスポンスには、ロールがorganizationにスコープされていることを示す`type: "organization"`と`owner_id`が含まれます。
  </Tab>
</Tabs>

<div id="learn-more">
  ## 詳しくはこちら
</div>

* [ロールに権限を追加する](/docs/ja-jp/manage-users/access-control/configure-core-rbac/roles/add-permissions-to-roles)
* [ロールから権限を削除する](/docs/ja-jp/manage-users/access-control/configure-core-rbac/roles/remove-permissions-from-roles)
* [ユーザーにロールを割り当てる](/docs/ja-jp/manage-users/access-control/configure-core-rbac/rbac-users/assign-roles-to-users)
* [ロールを削除する](/docs/ja-jp/manage-users/access-control/configure-core-rbac/roles/delete-roles)